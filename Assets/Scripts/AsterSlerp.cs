using UnityEngine;
using Random = System.Random;

/// <summary>
/// </summary>
public class AsterSlerp : MonoBehaviour
{
}



/*   for(int i = 0; i < 100; ++i )
        {
    
     GameObject newAsteroid = (GameObject)Instantiate(asteroidPreFab, Random.insideUnitSphere * fieldRadius, Random.rotation);
     float size = Vector3.Random (0.3f, 2);
    newAsteroid.transform.localScale = Vector3.one* size;
    // if the asteroid has a rigidbody...
        newAsteroid.GetComponent<Vector3>().z  <Rigidbody.velocity> = Random.insideUnitSphere* movementSpeed;
        newAsteroid.GetComponent<Vector3>().y <Rigidbody.Instantiate( newAsteroid = sizeof(int)Random.insideUnitSphere* rotationSpeed>;
    }*/
